<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a1a1a;
            /* Darker background */
            color: #fff;
            /* Adjusted text color for better readability */
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ffffff;
            /* Lighter neon green border */
            border-radius: 10px;
            background-color: rgba(255, 255, 255, 0.05);
            /* Transparent background */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            /* Holographic shadow effect */
            margin-top: 50px;
            margin-bottom: 50px;
            position: relative;
            /* Allow positioning for child elements */
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .review-info p {
            font-size: 1.1em;
            margin-bottom: 15px;
        }

        .review-info p b {
            color: #ffcc00;
            /* Yellow text for bold elements */
            font-weight: bold;
            /* Make text bold */
        }

        .review-info p:not(:nth-child(odd)) {
            color: #fff;
            /* White text for non-bold elements */
        }

        .review-info {
            margin-bottom: 30px;
            /* Increase bottom margin for spacing */
        }

        .back-link {
            position: absolute;
            bottom: 30px;
            /* Adjust bottom positioning */
            right: 30px;
            /* Adjust right positioning */
            color: #fff;
            /* White text */
            background-color: #4caf50;
            /* Green background color */
            padding: 10px 30px;
            /* Adjust padding */
            font-size: 1.2em;
            /* Larger font size */
            text-decoration: none;
            /* Remove underline */
            border-radius: 12px;
            /* Rounded corners */
            transition: background-color 0.3s ease;
            /* Smooth transition for background color */
        }

        .back-link:hover {
            background-color: #66bb6a;
            /* Lighter green on hover */
        }

        .navbar-brand {
            font-weight: bold;
        }

        .navbar {
            font-size: 1.2rem;
            padding-top: 25px;
            padding-bottom: 30px;
        }

        .navbar .navbar-brand {
            font-size: 1.5rem;
            margin: auto;
        }

        .form-title {
            font-size: 2em;
            font-weight: bold;
            margin-bottom: 20px;
            text-align: center;
            color: #66ff33;
            /* Lighter neon green color */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            /* Text shadow */
        }
    </style>
    <title>Event Details</title>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">&#9757;GitBiz</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </nav>

    <div class="container">
        <div class="header">
            <h2 class="form-title">Event Details</h2>
        </div>
        <% if (event) { %>
            <div class="student-info">
                <p style="color: #ffffff;"><b style="color: #ffcc00;">Event Title:</b><br>
                    <%= event.title %>
                </p>
                <div class="student-image" style="color: #ffcc00; border: 1px solid #ffcc00; padding: 10px;">
                    <b>Event Image:</b><br><br>
                    <img src="/images/<%= event.image %>" alt="<%= event.title %>" style="max-width: 100%; height: auto;">
                </div>
                <br>
                <p style="color: #ffffff;"><b style="color: #ffcc00;">Date & Time:</b><br>
                    <%= new Date(event.date).toLocaleString('en-US', { month: 'short' , day: '2-digit' ,
                        year: 'numeric' , hour: '2-digit' , minute: '2-digit' }) %>
                </p>
                <p style="color: #ffffff;"><b style="color: #ffcc00;">Location:</b><br>
                    <%= event.location %>
                </p>
                <p style="color: #ffffff;"><b style="color: #ffcc00;">Description:</b><br>
                    <%= event.desc %>
                </p>
                <br><br><br>
            </div>
            <% } else { %>
                <p>No event found.</p>
                <% } %>
                    <a href="/" class="back-link">Back</a>
    </div>

</body>

</html>